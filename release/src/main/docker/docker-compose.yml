version: "3.9"

services:

  phonebook_db:
    container_name: phonebook_mysql
    restart: always
    image: mysql:latest
    environment:
      - MYSQL_DATABASE=phonebook
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_USER=user
      - MYSQL_PASS=pass
    ports:
      - "3306:3306"
    volumes:
      - phonebook-volume:/var/lib/mysql

  phonebook:
    container_name: phonebook_application
    depends_on:
      - phonebook_db
    image: phonebook:latest
    ports:
      - "8080:8080"
    environment:
      - spring.profiles.active=prod

volumes:
  phonebook-volume: